//Ejercicio 2:
void main (void)
{
 int NumeroA, NumeroB;
 // Ingresar el primer Numero
 printf("Indique NumeroA:");
 scanf("%d", & NumeroA);
 // Ingresar el segundo numero
 printf("Indique NumeroB:");
 scanf("%d", & NumeroB);
 // Determinar el mayor numero
 if (NumeroA <NumeroB)
 {printf("El numero mayor es %d", NumeroB);
 }
 if (NumeroA >NumeroB)
 {printf("El numero mayor es %d", NumeroA);
 }
}